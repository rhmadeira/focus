import styled from "@emotion/styled";

export const ContainerAll = styled.div`
  background: red;
`;

export const Title = styled.h3`
  font-size: 1.2rem;
  font-weight: 400;
  margin: 0;
  padding: 0;
`;
